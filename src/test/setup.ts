// Vitest jsdom setup — runs before every test file.
//
// jsdom provides window/document/localStorage; nothing extra is needed for the
// current unit tests. Kept as a hook for future matchers/polyfills.
export {}
